# oms-v6

## Description
Spring Boot application generated by Spring Boot Integration Agent.

## Project Structure
```
oms-v6/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/example/omsv6/
│   │   │       ├── Application.java
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       └── model/
│   │   └── resources/
│   │       ├── application.properties
│   │       ├── static/
│   │       └── templates/
│   └── test/
│       └── java/
├── pom.xml
└── README.md
```

## How to Run

### Prerequisites
- Java 17 or higher
- Maven 3.6+

### Build the project
```bash
mvn clean install
```

### Run the application
```bash
mvn spring-boot:run
```

Or run the JAR file:
```bash
java -jar target/oms-v6-0.0.1-SNAPSHOT.jar
```

## API Endpoints

The application runs on `http://localhost:8080`

### Swagger UI (API Documentation)
- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **API Docs (JSON)**: http://localhost:8080/api-docs
- **API Docs (YAML)**: http://localhost:8080/api-docs.yaml

### Available Endpoints:
- **POST** `/api/orders`
- **GET** `/api/orders/{id}`
- **GET** `/api/orders`
- **PUT** `/api/orders/{id}/status`
- **DELETE** `/api/orders/{id}`

## Testing

Run tests with:
```bash
mvn test
```

## Technology Stack
- Java 17
- Spring Boot 3.2.5
- Maven
- SpringDoc OpenAPI (Swagger UI)
